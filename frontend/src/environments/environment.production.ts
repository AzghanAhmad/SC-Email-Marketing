export const environment = {
  production: true,
  // Set in Railway frontend service: RAILWAY_PUBLIC_DOMAIN or override at build time
  apiUrl: 'https://sc-email-marketing-production.up.railway.app/api/v1',
};
